<%= form_for(@edbguest) do |f| %>
  <% if @edbguest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edbguest.errors.count, "error") %> prohibited this edbguest from being saved:</h2>

      <ul>
      <% @edbguest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_field :note %>
  </div>
  <div class="control-group">
    <%=f.label :guest_id, class: 'control.label' %>
    <div class="controls">
      <%= collection_select( :edbguest, :guest_id, Guest.all, :id, :id, {}, { :multiple => false }) %>
    </div>
  </div>
  <div class="control-group">
    <%=f.label :edb_id, class: 'control.label' %>
    <div class="controls">
      <%= collection_select( :edbguest, :edb_id, Edb.all, :id, :id, {}, { :multiple => false }) %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
